[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -q
markers =
    host_integration: requires the deployed desktop/browser session and network
    subprocess_integration: launches and terminates a nested Norax runtime
